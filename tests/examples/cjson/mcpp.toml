# cJSON test project: depends on compat.cjson (built from source via this repo's
# own index) and asserts behavior under `mcpp test`. Part of the mcpp-index
# self-referential workspace.
[package]
name = "cjson-tests"
version = "0.1.0"

[indices]
compat = { path = "../../.." }

[dependencies.compat]
cjson = "1.7.19"
