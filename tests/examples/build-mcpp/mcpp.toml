# L3 build.mcpp consumer: the project-local build.mcpp generates a source and
# emits a compile define; the test asserts both took effect. Exercises the
# build.mcpp pipeline itself (no registry dependency).
[package]
name    = "build-mcpp-tests"
version = "0.1.0"
